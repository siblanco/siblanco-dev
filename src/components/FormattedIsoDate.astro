---
interface Props {
  date: Date;
}

const { date } = Astro.props;

const options = {
  year: "numeric",
  month: "long",
  day: "numeric",
};
---

{new (Date as any)(date).toLocaleDateString(undefined, options)}
